<!-- Page to use the Add to Cadence component from the Referral Account

   History
   -------
   06/20/2022 Dan Carmen        Created

 -->
<apex:page standardController="Referral_Account__c"
           extensions="AddToCadenceController"
           id="cadencePageRefAccount"
           showHeader="false"
           lightningStylesheets="true"
           sidebar="false">
     <c:AddToCadence recordId="{!Referral_Account__c.Id}" selectedRec="{!rec}" selectedRecs="{!recs}" formType="Record" startObjectType="Referral_Account__c" />
</apex:page>