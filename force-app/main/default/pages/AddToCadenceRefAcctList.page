<!-- Page to use the Add to Cadence component from a Referral Account List view

   History
   -------
   06/20/2022 Dan Carmen        Created

 -->

<apex:page standardController="Referral_Account__c"
           recordSetVar="Referral_Accounts__c"
           extensions="AddToCadenceController"
           id="cadencePageRefAccounts"
           showHeader="false"
           lightningStylesheets="true"
           sidebar="false">
     <c:AddToCadence recordId="{!Referral_Account__c.Id}" selectedRec="{!rec}" selectedRecs="{!recs}" formType="List" startObjectType="Referral_Account__c" />
</apex:page>