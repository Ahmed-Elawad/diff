<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Pass_to_New_Loads_Requirements_2</fullName>
    <active>true</active>
    <description>Benefit Option must be populated before Pass to New Loads is checked.</description>
    <errorConditionFormula>Pass_to_New_Loads__c =true &amp;&amp; IsBlank(Text( Medical_Option__c )) &amp;&amp; NOT(BEGINS($Profile.Name,&quot;System Administrator&quot;))</errorConditionFormula>
    <errorDisplayField>Medical_Option__c</errorDisplayField>
    <errorMessage>Medical Option must be populated before Pass to New Loads is checked.</errorMessage>
</ValidationRule>
