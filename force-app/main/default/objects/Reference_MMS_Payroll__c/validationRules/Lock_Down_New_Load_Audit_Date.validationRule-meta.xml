<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Lock_Down_New_Load_Audit_Date</fullName>
    <active>true</active>
    <description>Prevents the New Load Audit Date field from being updated multiple times.</description>
    <errorConditionFormula>ISCHANGED(New_Load_Audit_Date__c )  &amp;&amp;  NOT(ISNULL(PRIORVALUE(New_Load_Audit_Date__c ))) &amp;&amp; NOT(BEGINS($Profile.Name,&quot;Field Ops Manager&quot;))</errorConditionFormula>
    <errorDisplayField>New_Load_Audit_Date__c</errorDisplayField>
    <errorMessage>Only the Implementation Supervisor or Manager can modify the New Load Audit Date field after the initial entry.</errorMessage>
</ValidationRule>
