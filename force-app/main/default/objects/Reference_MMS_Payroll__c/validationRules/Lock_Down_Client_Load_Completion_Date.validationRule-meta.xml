<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Lock_Down_Client_Load_Completion_Date</fullName>
    <active>true</active>
    <description>Prevents the Client Load Completion Date field from being updated multiple times.</description>
    <errorConditionFormula>ISCHANGED(Client_Load_Completion_Date__c)  &amp;&amp;  NOT(ISNULL(PRIORVALUE(Client_Load_Completion_Date__c))) &amp;&amp; NOT(BEGINS($Profile.Name,&quot;Field Ops Manager&quot;))</errorConditionFormula>
    <errorDisplayField>Client_Load_Completion_Date__c</errorDisplayField>
    <errorMessage>Only the Implementation Supervisor or Manager can modify the Client Load Completion Date field after the initial entry.</errorMessage>
</ValidationRule>
