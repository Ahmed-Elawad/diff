<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Force_HoldNotes_populated</fullName>
    <active>true</active>
    <description>Force users to populate the Hold Notes field, when the Agency Hold Placed field is populated.</description>
    <errorConditionFormula>AND( 
($RecordType.Name = &quot;Reference PAC Record Type&quot; ), 
(LEN(Hold_Notes__c) = 0),
(NOT(ISPICKVAL(Agency_Hold_Placed__c, &apos;&apos;))),
($Profile.Name &lt;&gt;&quot;System Administrator&quot;) 
)</errorConditionFormula>
    <errorDisplayField>Hold_Notes__c</errorDisplayField>
    <errorMessage>You must enter a value in the Hold Notes field, when the Agency Hold Placed field is populated.</errorMessage>
</ValidationRule>
