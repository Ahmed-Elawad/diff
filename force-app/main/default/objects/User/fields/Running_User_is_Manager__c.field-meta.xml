<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Running_User_is_Manager__c</fullName>
    <businessStatus>Active</businessStatus>
    <description>For use in reporting. Adding as a filter with a True value will return Users where the running user is in the Manager field of a User Record (DSM use only)</description>
    <formula>Manager.Id = $User.Id</formula>
    <inlineHelpText>For use in reporting. Adding as a filter with a True value will return Users where the running user is in the Manager field of a User Record (DSM use only)</inlineHelpText>
    <label>Running User is Manager</label>
    <securityClassification>Internal</securityClassification>
    <type>Checkbox</type>
</CustomField>
