<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>First_Attempt_Date</fullName>
    <active>true</active>
    <description>Attempt Date must be today&apos;s date</description>
    <errorConditionFormula>AND( ISNULL(PRIORVALUE( X1st_Attempt__c )),
ISCHANGED(  X1st_Attempt__c ),

NOT( X1st_Attempt__c = TODAY()))</errorConditionFormula>
    <errorDisplayField>X1st_Attempt__c</errorDisplayField>
    <errorMessage>Attempt Date must be today&apos;s date.</errorMessage>
</ValidationRule>
