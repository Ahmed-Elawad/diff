<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Must_select_Type_of_Discrepancy</fullName>
    <active>true</active>
    <description>If Discrepancy is Yes, must select a Type of Discrepancy</description>
    <errorConditionFormula>AND( ISPICKVAL( Discrepancy__c , &apos;Yes&apos;) , ISBLANK(TEXT(Type_of_Discrepancy__c)))</errorConditionFormula>
    <errorMessage>If Discrepancy is Yes, must select a Type of Discrepancy</errorMessage>
</ValidationRule>
