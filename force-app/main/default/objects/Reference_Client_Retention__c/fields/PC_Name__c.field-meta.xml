<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PC_Name__c</fullName>
    <businessStatus>Active</businessStatus>
    <description>This is only used to create a searchable field, so CRT can global search on account name to see if other refObj exists.

Should NOT be on any page layout</description>
    <encryptionScheme>None</encryptionScheme>
    <externalId>false</externalId>
    <inlineHelpText>This is only used to create a searchable field, so CRT can global search on account name to see if other refObj exists.

Should NOT be on any page layout</inlineHelpText>
    <label>PC Name</label>
    <length>255</length>
    <required>false</required>
    <securityClassification>Internal</securityClassification>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
