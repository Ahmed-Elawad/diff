<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>EligibleToEmail__c</fullName>
    <businessStatus>Active</businessStatus>
    <description>Used to verify whether Lead can be emailed through Touchpoint process</description>
    <formula>AND(ISNULL( EmailBouncedDate ) ,ISNULL(  Email_Verification_Date__c ), NOT(HasOptedOutOfEmail), NOT(No_Longer_With_Company__c), NOT( ISBLANK(Email)))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>EligibleToEmail</label>
    <securityClassification>Internal</securityClassification>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
