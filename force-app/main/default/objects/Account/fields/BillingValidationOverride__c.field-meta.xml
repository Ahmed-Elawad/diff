<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>BillingValidationOverride__c</fullName>
    <defaultValue>false</defaultValue>
    <inlineHelpText>This is for if we want to override the enterprise address validation for this address. This should only be checked if the address comes back with an error from the validation service but the users want to use this address anyway.</inlineHelpText>
    <label>Billing Validation Override</label>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
