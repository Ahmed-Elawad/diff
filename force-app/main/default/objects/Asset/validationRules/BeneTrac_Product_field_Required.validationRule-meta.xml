<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>BeneTrac_Product_field_Required</fullName>
    <active>true</active>
    <description>If the BeneTrac Operations profile is creating this product the BeneTrac Product Name field is required</description>
    <errorConditionFormula>AND(
ISNEW(),
($Profile.Name = &quot;BeneTrac Operations&quot;),  ISBLANK(TEXT(BeneTrac_Products__c ))
)</errorConditionFormula>
    <errorMessage>If you are creating a new BeneTrac Product and Service record you must populate the BeneTrac Products field.</errorMessage>
</ValidationRule>
