<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Running_User_is_User__c</fullName>
    <businessStatus>Active</businessStatus>
    <description>For use in reporting. Adding as a filter with a True value will return records where the running user is in the User field</description>
    <formula>User__r.Id = $User.Id</formula>
    <inlineHelpText>For use in reporting. Adding as a filter with a True value will return records where the running user is in the User field</inlineHelpText>
    <label>Running User is User</label>
    <securityClassification>Internal</securityClassification>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
