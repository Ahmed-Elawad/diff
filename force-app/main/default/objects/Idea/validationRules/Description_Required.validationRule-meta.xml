<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Description_Required</fullName>
    <active>true</active>
    <description>for some reason, ISBLANK doesn&apos;t work and required on page layout doesn&apos;t work</description>
    <errorConditionFormula>AND(NOT(BEGINS($Profile.Name,&apos;System Admin&apos;)),LEN(Body)=0)</errorConditionFormula>
    <errorMessage>Each Idea must have a description entered.</errorMessage>
</ValidationRule>
