<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Edit_Restrictions</fullName>
    <active>true</active>
    <description>Only the creator can edit the Title and Body of an Idea.</description>
    <errorConditionFormula>AND(
OR(ISCHANGED(Body),ISCHANGED(Title )),
NOT(BEGINS($Profile.Name,&apos;System Admin&apos;)),
CreatedById &lt;&gt; $User.Id )</errorConditionFormula>
    <errorMessage>Only the creator can edit the Title and Body of an Idea.</errorMessage>
</ValidationRule>
