<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Due_Date_Change_Rule</fullName>
    <active>false</active>
    <description>Should not allow to change the due date once populated</description>
    <errorConditionFormula>AND( RecordType.Name == &apos;2008-09 Premier Safety Task Record Type&apos;, 
!ISNEW() , 
ISCHANGED(ActivityDate),
   NOT(CONTAINS($Profile.Name,&apos;System Administrator&apos;))  
)</errorConditionFormula>
    <errorMessage>Cannot change the activity date once populated</errorMessage>
</ValidationRule>
